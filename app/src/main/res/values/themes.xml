<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.COEN390_SafeHit" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        //green
        <item name="colorPrimary">@color/green</item>

        <item name="colorAccent">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:colorButtonNormal">@color/white</item>
        <item name="android:textColorHighlightInverse">@color/white</item>
        <item name="android:textColorHighlight">@color/white</item>
        <item name="android:popupBackground">@android:color/transparent
        </item> <!-- Make the dropdown background transparent -->
        <item name="buttonBarPositiveButtonStyle">@color/black</item>
        <item name="buttonBarNegativeButtonStyle">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <!--        <item name="android:windowTranslucentStatus">true</item>-->
    </style>

    <style name="Theme.COEN390_SafeHit" parent="Base.Theme.COEN390_SafeHit" />

    <style name="Second.Theme.COEN390_SafeHit" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here -->
        ...
    </style>

</resources>